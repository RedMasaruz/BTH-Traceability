# คู่มือการติดตั้งและเผยแพร่ PWA

## 📋 ข้อกำหนดเบื้องต้น

- ✅ เว็บไซต์ต้องทำงานบน HTTPS (ยกเว้น localhost)
- ✅ ต้องมีไฟล์ manifest.json
- ✅ ต้องมี Service Worker ที่ลงทะเบียนแล้ว
- ✅ ต้องมีไอคอนขนาด 192x192 และ 512x512 พิกเซล

## 🚀 วิธีการเผยแพร่บน GitHub Pages

### ขั้นตอนที่ 1: เตรียมไฟล์

```bash
# ตรวจสอบว่ามีไฟล์ที่จำเป็นครบถ้วน
ls -la manifest.json sw.js icons/
```

### ขั้นตอนที่ 2: Push โค้ดขึ้น GitHub

```bash
git add .
git commit -m "Deploy PWA to GitHub Pages"
git push origin main
```

### ขั้นตอนที่ 3: เปิดใช้งาน GitHub Pages

1. ไปที่ repository settings
2. เลื่อนไปที่ "Pages" ในเมนูด้านซ้าย
3. เลือก Source: "Deploy from a branch"
4. เลือก Branch: "main" หรือ "master"
5. เลือก Folder: "/ (root)"
6. คลิก "Save"

### ขั้นตอนที่ 4: รอให้ Deploy เสร็จ

- GitHub จะใช้เวลา 1-5 นาทีในการ deploy
- ดูสถานะได้ที่แท็บ "Actions"
- เมื่อเสร็จแล้วจะแสดง URL: `https://username.github.io/repository-name/`

### ขั้นตอนที่ 5: ปรับแต่ง Path (ถ้าจำเป็น)

ถ้า repository ไม่ได้อยู่ที่ root URL ต้องแก้ไข:

**manifest.json:**
```json
{
  "start_url": "/repository-name/",
  "scope": "/repository-name/"
}
```

**sw.js:**
```javascript
const urlsToCache = [
  '/repository-name/',
  '/repository-name/index.html',
  '/repository-name/manifest.json'
];
```

**index.html:**
```html
<link rel="manifest" href="/repository-name/manifest.json">
<script>
navigator.serviceWorker.register('/repository-name/sw.js')
</script>
```

## 🌐 การเผยแพร่บนเว็บโฮสติ้งอื่นๆ

### Netlify

1. เชื่อมต่อ GitHub repository กับ Netlify
2. ตั้งค่า Build Command: (ไม่ต้องใส่หากเป็น static site)
3. ตั้งค่า Publish Directory: `/`
4. Deploy!

### Vercel

```bash
npm install -g vercel
vercel
```

### Firebase Hosting

```bash
npm install -g firebase-tools
firebase login
firebase init hosting
firebase deploy
```

## 🔒 การตั้งค่า HTTPS

PWA จำเป็นต้องใช้ HTTPS ยกเว้น localhost

### GitHub Pages
- HTTPS เปิดอัตโนมัติ

### Custom Domain
1. ไปที่ Settings > Pages
2. ใส่ Custom domain
3. เช็ค "Enforce HTTPS"

## ✅ การทดสอบหลัง Deploy

### 1. ทดสอบ Manifest

เปิด: `https://your-domain.com/manifest.json`

ควรเห็น JSON ที่ถูกต้อง

### 2. ทดสอบ Service Worker

1. เปิด Chrome DevTools (F12)
2. ไปที่แท็บ "Application"
3. เลือก "Service Workers"
4. ควรเห็น SW ที่ Active

### 3. ทดสอบการติดตั้ง

#### Android
- เปิดด้วย Chrome
- ดูที่แถบ URL ว่ามีไอคอน "ติดตั้ง" หรือไม่

#### iOS  
- เปิดด้วย Safari
- แชร์ > เพิ่มไปยังหน้าจอหลัก

### 4. ทดสอบออฟไลน์

1. เปิดเว็บไซต์
2. เปิด DevTools > Network > Offline
3. รีเฟรชหน้า
4. ควรยังเห็นหน้าเว็บได้

## 📱 การทดสอบบนอุปกรณ์จริง

### Android (Chrome)

```bash
# เปิด Remote Debugging
chrome://inspect
```

### iOS (Safari)

1. เปิดการพัฒนาบน iPhone (Settings > Safari > Advanced > Web Inspector)
2. เชื่อมต่อกับ Mac
3. เปิด Safari > Develop > [Device Name]

## 🐛 การแก้ปัญหา

### ปัญหา: ติดตั้งไม่ได้

- ✅ ตรวจสอบว่าใช้ HTTPS
- ✅ ตรวจสอบ manifest.json ถูกต้อง
- ✅ ตรวจสอบมีไอคอน 192x192 และ 512x512
- ✅ Service Worker ลงทะเบียนสำเร็จ

### ปัญหา: อัพเดทไม่ทำงาน

```javascript
// ใน SW, เพิ่ม version
const CACHE_NAME = 'bth-farmer-v2'; // เปลี่ยนเลขเวอร์ชัน
```

### ปัญหา: ออฟไลน์ไม่ทำงาน

- ตรวจสอบ SW มีการแคชไฟล์ที่ต้องการ
- ลอง Hard Refresh (Ctrl+Shift+R)
- ล้างแคชและลองใหม่

## 📊 เครื่องมือตรวจสอบ PWA

### Lighthouse Audit

1. เปิด Chrome DevTools
2. ไปที่แท็บ "Lighthouse"
3. เลือก "Progressive Web App"
4. คลิก "Generate report"

### PWA Builder

https://www.pwabuilder.com/

ใส่ URL แล้วจะวิเคราะห์และแนะนำการปรับปรุง

## 📈 การติดตามผลการใช้งาน

### Google Analytics

```html
<!-- เพิ่มใน index.html -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

### PWA Analytics Events

```javascript
// Track install
window.addEventListener('appinstalled', () => {
  gtag('event', 'pwa_installed');
});

// Track offline usage
window.addEventListener('offline', () => {
  gtag('event', 'pwa_offline');
});
```

## 🔄 การอัพเดท PWA

1. แก้ไขโค้ด
2. เพิ่มเลขเวอร์ชันใน SW
3. Push และ Deploy
4. SW จะตรวจจับและแจ้งผู้ใช้อัพเดท

## 📞 ติดต่อสอบถาม

หากมีปัญหาหรือข้อสงสัย ติดต่อ:
- LINE: @bthcenter
- โทร: 092-4579929, 063-5033042
